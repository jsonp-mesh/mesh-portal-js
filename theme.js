import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#037FFF',
    },

    secondary: {
      main: '#F7C366',
    },
    tertiary: {
      main: '#F6B94C',
    },
  },
});

export default theme;